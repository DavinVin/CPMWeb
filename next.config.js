/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Ketika foto asli sudah siap, tambahkan domain di sini
    // contoh: domains: ['your-cdn.com']
  },
}

module.exports = nextConfig
